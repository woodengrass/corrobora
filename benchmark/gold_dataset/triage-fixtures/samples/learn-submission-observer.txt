侦测器朝向与更新

群友投稿：侦测器在检测到面前方块状态变化后，会在背面输出一个 2gt 的脉冲。
需要注意的是侦测器不是靠方块更新工作的，它每刻比较面前方块的状态，
所以隔着一格或者被压线的情况都不会让它误触发。

常见用法是拿侦测器做单向的信号提取，例如检测活塞是否推出、
检测作物是否成熟，或者在无法用比较器读取的地方替代比较器。

侦测器的朝向以放置时玩家面对的方向为准，输出端在带有红点的那一面。
放反了会让整条线路的时序整体偏移 2gt，排查时可以先确认朝向。

版本：Java 1.20.1 原版单人存档实测。
