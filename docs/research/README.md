# 研究歸檔：要做什麼、為什麼

結論先行：現在的候選架構裡，約六到七成已經被強模型和現成工具吃掉；
兩到三成是對但沒新意的工程（直接拿來用就好）；只剩一小塊值得投入——
**版本真相、依賴失效、三層出處、軌跡實驗室**。詳見 `00`；證偽見 `10`；點子見 `11`；出處全在 `SOURCES.md`。

## 文件

- `00-reassessment-2026-09-18.md`：第一輪總判斷（15 個問題一次答完）。
- `10-bet-verification-2026-09-19.md`：4 個 bets 有沒有人做過、差在哪。
- `11-ideation-shortlist-2026-09-19.md`：28 個還沒人產品化的點子。
- `SOURCES.md`：所有來源，內文只寫 `[S-編號]`（含早期 memory 報告 38 條的 F 組併入）。

## 怎麼讀

先看 `00` 的結論和分流（文件 / 程式 / 藍圖分開處理），再看 `10` 確認每塊的邊界，
最後從 `11` 挑先做的事。建議順序：版本 schema → MEME 複現 → 便宜 abstention → 藍圖 lint→模擬鏈。
